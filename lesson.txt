import random



class Job:
    def__init__(self, name:str = "test_job, salary:float = 1"):
         self.name = name
        self.salary = salary

    """сеттеры(изменяют значения параметров)"""
    # сеттер для имени
    def set_name(self, name:str):
        self.name = name

    # сеттер для зарплаты
    def set_salary(self, salary:float):
        self.salary = salary




class Human:
    def __init__(self, name:str = "name", job:Job = None,
                 home = None, car = None, money:float = 0):
        self.name = name
        self.job = job
        self.home = home
        self.happiness = 100
        self.satiety = 100
        self.car = car
        self.money = money

    def set_name(self, name:str):
        self.name = name

    def set_home(self, home):
        self.home = home

    def set_car(self, car):
        self.car = car

    def set_job(self, job:Job):
        self.job = job

    def eat(self):
        self.satiety += 10

    def work(self):
        self.money += self.job.salary




chef-povar = Job("chef-povar", 550)
dentist = Job("dentist", 5000)
dizayner = Job("dizayner", 4000)


Jale = Human("Jale", chef-povar, "mardakan_dacha", "MB_Maybach", 250)
Ahu = Human("Ahu", dentist, "badamdar_dacha", "07_peredok", 450)
Aysu = Human("Aysu", dizayner, "28may", "Porsche", 350)


people_list = [Jale, Ahu, Aysu]

Farkhad.eat()
Farkhad.work() 

programmist.set_salary(4000)
programmist.set_name("developer")

for person in people_list:
    print(f"Job:\n\tName: {person.job.name} \n\tSalary: {person.job.salary}")
    print(f"Human:\n\tName {person.name} \n\tJob: {person.job.name}({person.job.salary}) \n\tHome: {person.home} \n\tCar: {person.car} \n\tHappiness: {person.happiness} \n\tSatiety: {person.satiety} \n\tMoney: {person.money}")